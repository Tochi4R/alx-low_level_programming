#include "main.h"
/**
 * _abs - Computes the absolute value of an integer.
 * @int: The integer to compute the absolute value of.
 *
 * Return: The absolute value of num.
 */

int _abs(int)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
