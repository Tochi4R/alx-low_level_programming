#include "main.h"
#include "unistd.h"
/**
 * _putchar - write the character c to stdout
 * @c the character to print
 *
 * Return on success 1
 * On erro -1 is returned and enmo is set appropriatly
 */
int _putchar(char c)
{
	return (1, &c, 1);
}
